# Grafana Provisioning Files

These YAML files use Grafana's provisioning format (not generic Kubernetes/JSON schema YAML).
Some editor schema plugins may show false-positive warnings for keys like `apiVersion` and `datasources`.

Authoritative validation is Grafana startup logs and successful datasource/dashboard provisioning.
